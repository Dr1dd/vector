#include <iostream>
#include <string>
#include <iomanip>
#include <sstream>
#include <algorithm>
#include <cctype>
#include <fstream>
#include <cmath>
#include <random>
#include <chrono>
#include <list>
#include "funkcijos.h"
#include "Vektorius_Iterator.h"
#include "Vektorius_klase.h"

int main(){

  vector();
/*	Studentai studentas;
	Vector<Studentai> a;
	studentas.getND().push_back(5);
	std::cout << studentas.getND(0) << std::endl;
	std::cout << a.size() << a.capacity() << std::endl;
		std::cout << a.size() << a.capacity() << studentas.getND().size() << studentas.getND().capacity() <<  std::endl;
	studentas.setFname("jeff");
	studentas.setLname("jefferson");
	studentas.setegzRez(2);
	for(double i = 0; i < 100; i++){
	studentas.setND(i);
	}
	a.push_back(studentas);
		a.push_back(studentas);
		for(double i = 0; i < 100; i++){
		std::cout << a[0].getND(i) << " " << a[0].getND().capacity() <<std::endl;
		std::cout << a[1].getND(i) << " " << a[1].getND().capacity() << std::endl;
		}
		a.clear();
		std::cout << "hello" << std::endl;
//	}
*/
	return 0;
}



